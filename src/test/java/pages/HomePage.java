package pages;

public class HomePage extends BasePage{



    // Construtor / Construtor

    @FindBy(id = "user-name")
        WebElement
    public HomePage(WebDriver driver) {
        super(driver);
        this.driver = driver; // conecta o objeto interno e o externo do  Selenium

    @FindBY(id = )
    WebElement

        public HomePage(WebDriver driver){
        super(driver)
    }

    public vold String lerTituloDaPagina(){
        return lbtTituloPagina.getText(); // retorna o que estiver escrito no elemento
        }
    public void clicarNoTituloDoProduto(String productId){
            java.lang.String idDinamico = "item_" + produtc
        }





        // Funções e Metodos
}
